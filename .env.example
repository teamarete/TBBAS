# Flask Configuration
FLASK_ENV=production
FLASK_DEBUG=False

# Server Configuration (for local development)
HOST=127.0.0.1
PORT=5000

# Email Notifications
EMAIL_NOTIFICATIONS_ENABLED=True
NOTIFICATION_EMAIL=blood@teamarete.net
FROM_EMAIL=tbbas@teamarete.net

# SMTP Configuration (for Gmail, use app-specific password)
# Gmail: smtp.gmail.com:587
# SendGrid SMTP: smtp.sendgrid.net:587
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password

# Alternative: SendGrid API (recommended for production)
# Get free API key at https://sendgrid.com
SENDGRID_API_KEY=
